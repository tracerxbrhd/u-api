package dev.uapi.api.network;

/** Canonical protocol line for U-API 2.x network envelopes. */
public final class UApiNetworkProtocol {
    public static final ProtocolVersion CURRENT = new ProtocolVersion(2, 0);

    private UApiNetworkProtocol() {
    }
}
