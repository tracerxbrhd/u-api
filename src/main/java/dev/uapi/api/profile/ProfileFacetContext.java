package dev.uapi.api.profile;

/** Identifies the surface requesting a public, server-filtered profile projection. */
public enum ProfileFacetContext {
    PUBLIC_PROFILE,
    PLAYER_CARD,
    INSPECTION
}
