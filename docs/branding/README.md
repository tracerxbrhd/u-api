# U-API branding

The PNG files are the preferred publication/runtime sources. JPEG variants are retained as original distribution assets.

NeoForge uses the wide root resource `u_api_banner.png` as the mod-list logo with nearest-neighbor filtering. The square icon remains available at `assets/u_api/branding/icon.png`.
